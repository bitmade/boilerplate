{
  "name": "bitmade-boilerplate",
  "version": "2.2.0",
  "dependencies": {
    "style-helper": "github:bitmade/style-helper"
  },
  "devDependencies": {
    "bitmade-dev-tools": "github:bitmade/dev-tools",
    "cross-env": "^3.1.2",
    "webpack-config": "^3.1.0"
  },
  "scripts": {
    "dev": "bitmade-dev-server",
    "dev-fs": "bitmade-dev-server --build-assets",
    "build": "cross-env NODE_ENV=production bitmade-project-export --compile-assets",
    "export": "cross-env NODE_ENV=production bitmade-project-export --compile-assets --compile-templates"
  }
}
